# Python Source

Dieses Verzeichnis enthält den installierbaren Python-Quellcode der Demo-Suite.

## Struktur

- `ai_agent_patterns/`: Python-Package mit CLI, Provider-Konfiguration, Tools und Pattern-Demos

## Hinweise

Das Paket wird aus dem `code`-Verzeichnis per editable install eingebunden:

```bash
pip install -e .
```

